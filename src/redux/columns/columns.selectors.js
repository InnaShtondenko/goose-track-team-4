export const selectColumns = state => state.columns.columns.columns;

export const isLoading = state => state.columns.columns.isLoading;

export const selectError = state => state.columns.columns.error;
